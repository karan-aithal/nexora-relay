<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Windows-only adapter (winscard.dll / serial). Excluded from the Linux CI solution filter. -->
    <TargetFramework>net10.0-windows</TargetFramework>
  </PropertyGroup>

</Project>
